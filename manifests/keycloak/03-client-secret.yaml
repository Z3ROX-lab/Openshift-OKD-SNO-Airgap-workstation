apiVersion: v1
kind: Secret
metadata:
  name: keycloak-client-secret
  namespace: openshift-config
type: Opaque
stringData:
  # IMPORTANT: Ne jamais commiter la vraie valeur
  # Créer manuellement avec :
  # oc create secret generic keycloak-client-secret \
  #   --from-literal=clientSecret=<SECRET> \
  #   -n openshift-config
  clientSecret: PLACEHOLDER_REPLACE_ME
